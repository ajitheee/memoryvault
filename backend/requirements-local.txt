# Lean deps for running MemoryVault OFF Emergent (e.g. local Windows/macOS/Linux).
# Excludes emergentintegrations (private, not on public PyPI) and jq (fails to
# build on Windows and isn't imported). Extraction still works via the openai /
# heuristic backends — see RUN.md.
#
#   pip install -r requirements-local.txt
#
fastapi==0.110.1
uvicorn==0.25.0
motor==3.3.1
pymongo==4.6.3
python-dotenv>=1.0.1
pydantic>=2.6.4
email-validator>=2.2.0
pyjwt>=2.10.1
bcrypt==4.1.3
requests>=2.31.0
python-multipart>=0.0.9
